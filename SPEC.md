# SPEC.md — kofun-atlas-ai / Kofun Atlas AI

<!-- scaffold template v1.30.0 から展開(2026-09-08)。以後このファイルはプロジェクトが育てる -->

正本の構想は `Kofun_Atlas_AI_V1.0_Implementation_Spec.md`(以下「構想書」)。
本 SPEC は構想書を**実測で上書きした運用仕様**である。両者が食い違う箇所は §3 に一覧する。

本 SPEC の断定形は次の三区分のいずれかで書く(HC-077)。

- **実測** — 値と実測日を書く。母集団(何を・何件走査して)も併記する
- **見込み** — 「見込み」と明示し、確かめる手段を併記する
- **実装の説明** — 実物に当てて確かめてから書く

---

## 1. 目的

日本の古墳について、再配布可能な公開データだけを統合し、
**地図・年代・地形・AI 特徴量**の四つの軸で探索できる静的 Web アプリを作る。

中心にあるのは「古墳を並べること」ではなく、**一件ごとに
「どこから来た値か・どれくらい確かか・欠けているか」を追跡できること**である。
古墳研究では情報が欠けていること自体が情報なので、欠測を推測で埋めず、
公表値・派生値・AI 推定値を画面上で分離する。

## 2. 母集団と採録範囲(実測 2026-09-08)

| 事項 | 値 | 出所 |
|---|---|---|
| Geoshape POI CSV | 5,576,569 バイト / sha256 `d4cd6beb…` / Last-Modified 2025-05-15 | 実測 |
| CSV 全行 | 32,038 行 | 実測 |
| `分類3 = 古墳` | **2,808 件** | 実測 |
| うち座標欠測 | 0 件 | 実測 |
| うち読み欠測 | 0 件 | 実測 |
| `id` の一意性 | 32,038 行すべて一意(重複 0) | 実測 |
| 都道府県の広がり | 47 都道府県すべてに分布(最多 30番=245 件) | 実測 |
| 同名グループ | 129 組(最頻「大塚古墳」14 件) | 実測 |
| 同一座標グループ | 82 組・のべ 185 行 | 実測 |

構想書 §2.2 の「分類『古墳・貝塚』は 3,287 件」は、`分類3` で分けると
**古墳 2,808 + 貝塚 479 = 3,287** の和であることを実測で確認した(2026-09-08)。
貝塚は古墳ではないので採録しない。

### 採録する / しない

| `分類3` | 件数 | V1.0 | 理由 |
|---|---|---|---|
| 古墳 | 2,808 | **採録** | 母集団 |
| 貝塚 | 479 | 除外 | 古墳ではない(構想書 §11.1 も除外を指示) |
| 天皇陵 | 23 | 除外 | **23 件中 22 件が平安期以降の天皇陵・火葬塚**(順徳・土御門・花園・醍醐・崇徳 等)で古墳ではない。唯一 `御廟野`(天智天皇陵・終末期古墳)が該当しうるが、1 件のために分類規則を緩めない(実測 2026-09-08・全 23 件を目視) |
| 横穴群 | 86 | 除外 | 横穴墓群であり墳丘を持たない。構想書 §9.1 の墳形分類に受け皿が無い。V1.1 候補 |

**2,808 件は構想書 §57 の公開条件「総古墳数 >= 2,000」を満たす**(実測)。

## 3. 構想書からの変更(すべて実測が根拠)

### 3.1 名称正規表現による候補抽出は採用しない

構想書 §11.1 は `(古墳|古墳群|塚古墳|前方後円墳|前方後方墳)` で候補を作れとするが、
このデータセットでは**足すものが無く、落とすものがある**。

- 名称に「古墳」を含むが `分類3 ≠ 古墳` の行: **0 件**(全 32,038 行走査・実測 2026-09-08)
- `分類3 = 古墳` だが名称に「古墳」を含まない行: **183 件**(2,808 件中 6.5%)
  — 例: 竹の花横穴 / 姥石向二号墳 / 元島名将軍塚 / 唐御所横穴

したがって**候補集合は `分類3` だけで決める**。名称は候補生成に使わない。

### 3.2 都道府県・市区町村は座標から決める(CSV の県名も県コードも使わない)

Geoshape CSV の `都道府県コード` と `住所` 先頭の県名が
**1,571 / 2,808 件(56.0%)で食い違う**(実測 2026-09-08)。
差は例外なく「住所の県 = コード − 1」で、他のずれ方は無い。

どちらが正しいかは行ごとに違う。国土地理院逆ジオコーダ
(`mreversegeocoder.gsi.go.jp/reverse-geocoder/LonLatToAddress`)が返す `muniCd` を真値として:

- 不一致 1,571 件から無作為 30 件(seed 20260908)を判定 → **30 件とも住所側が正・コード側が誤**
- ただし `id=240000301100`(外山古墳群・住所「愛知県 伊賀市 円徳院」)は逆で、
  逆ジオコーダは `muniCd=24216`(三重県伊賀市)を返す → **住所側の県名が誤・コード側が正**

**L1 で全件を通したあとの内訳**(実測 2026-09-08・母集団は出荷レコード 2,805 件):

| 事項 | 件数 | 割合 |
|---|---|---|
| 県コードが誤(逆ジオコーダと不一致) | **1,569** | 55.9% |
| 住所の県名が誤(逆ジオコーダと不一致) | **1**(外山古墳群のみ) | 0.0% |
| **二欄とも逆ジオコーダと食い違う** | **0** | 0.0% |
| 二欄とも一致 | 1,235 | 44.0% |

住所の県名は 2,805 件中 2,804 件で正しく、県コードは 1,236 件でしか正しくない。
**それでも「住所だけを採る」は採らない** —— 外山古墳群の 1 件が落ちるからである。
「二欄とも食い違うレコードが 0 件」は逆ジオコーディングの健全性検査になる(T-028)。
CSV の県欄は逆ジオコーダに渡していない(渡すのは座標だけ)ので循環しない。

### 沖縄県は 0 件になる(逆ジオコーディングの独立確認)

CSV の県コードが `47`(沖縄県)だったレコードは 11 件あるが、**全 11 件が鹿児島県だった**
(実測 2026-09-08。CSV 自身の住所も 11 件とも「鹿児島県」と書いている)。
確定後の都道府県は **46 種**で、沖縄県が 0 件になる。

これは古墳時代の文化圏が琉球列島に及んでいないという外部の事実と合う。
**この事実は逆ジオコーディングの入力に一切使っていない**ので、
「県コードをそのまま信じると沖縄県に 11 基の古墳が現れる」ことの独立した反証になっている。

**両フィールドとも単独では信頼できない。** よって `prefecture` / `municipality` は
座標から逆ジオコーダで決め、`muniCd` を正本として保持する。CSV の県名・県コードは
`raw_extra` に退避するだけで採用しない。逆ジオコーダが答えを返さなかった件は
`null` のまま残し、件数を Sources ページに出す。

名称は同じ国土地理院の市区町村表 <https://maps.gsi.go.jp/js/muni.js> で引く
(`muniCd` → 県コード・県名・市区町村コード・市区町村名)。
逆ジオコーダは 5 桁ゼロ詰めの `01217` を返し、表の鍵は `1217` なので、
**引く前にゼロを落として正規化する**(実測 2026-09-08)。

**逆ジオコーダの `{}` は障害ではなく「陸上でない」という答えである。**
404 と同じく再試行してはならない(HC-221)。取得結果は
`data/raw/gsi/revgeo.jsonl` に追記して**リポジトリに同梱する** ——
CI も他人のクローンも、一度も国土地理院を叩かずに同じ結果を再現できるようにする。

### 3.3 Wikidata は自動取得しない(人手スナップショット)

構想書 §2.3 / §11.2 は `query.wikidata.org/sparql` からの自動取得を指示するが、
**`https://query.wikidata.org/robots.txt` は `Disallow: /sparql` である**(実測 2026-09-08)。
`https://www.wikidata.org/robots.txt` も `User-agent: *` に対し `/w/` と `/api/` を Disallow しており、
Action API / Special:EntityData も自動巡回の対象外である。

したがって Wikidata は**文化庁 CSV と同じ人手スナップショット方式**にする。

- リポジトリは SPARQL 本文と取得手順(`data/raw/wikidata/README.md`)だけを持つ
- 人が WDQS の画面で実行し、結果を `data/raw/wikidata/YYYY-MM-DD/` に置く
- CI からは一切取得しない

### 3.4 文化庁 CSV は構想書どおり人手スナップショット

利用規約(2026-09-08 実測)は
「**文字情報**: 出典を記載の上、自由にご利用ください / **画像**: 第三者が権利を有するため作品毎に個別の許諾が必要」。
V1.0 は文字情報のみを使い、画像は一切転載しない。

### 3.5 地形は z14 を基本解像度にする

`±1000 m` 窓(`local_relief_1000m` が要求する最大窓)を満たすのに必要なタイル数(実測 2026-09-08):

| ズーム | 1px の地上距離(北緯 35°) | 1 件あたり必要タイル(中央値/最大) | 2,808 件のユニーク合計 |
|---|---|---|---|
| z14 | 7.83 m | 4 / 6 | **6,575** |
| z15 | 3.91 m | 9 / 16 | 16,737 |

V1.0 は **z14** を基本にする。DEM10B(10 m メッシュ)が全国を覆う解像度と釣り合っており、
取得量が 2.5 分の 1 で済む。

### 層は画素単位で重ねる(L2 実測)

**「タイルが存在する ⇒ 値がある」は偽である。** DEM5A はタイルが 200 を返しても、
その中身の大半が NoData のことがある。

- 石馬谷古墳(鳥取県米子市 35.451941, 133.442738)の中心タイル(z14):
  **DEM5A は 82.1% が欠測で中心画素も NaN**。同じタイルの DEM10B は欠測 0% で 29.17 m
- 最初の実装は「そのタイルが存在する最初の層」で決めていたため、
  2,805 件中 59 件が欠測 50% 以上になり、数件は標高そのものが取れなかった
- 欠測 20% 超の DEM5A タイル 617 枚から無作為 30 枚(seed 20260908)を調べると、
  **DEM10B は 30/30 で存在**するが、**欠測を完全に埋められるのは 10/30** ——
  残りは水域で、DEM10B にも標高が無い。DEM5B は 6/30、DEM5C は 7/30 で存在

したがって層は **画素単位で合成する**。優先順(5A → 5B → 5C → 10B)の上位を土台にし、
NaN の画素だけを下位層で埋める。**どの層にも値が無い画素は NaN のまま残す** ——
水面には標高が無く、0 で埋めれば「標高 0 の崖」が生まれる(構想書 §14.5)。

合成後の内訳(実測 2026-09-08・必要タイル 6,597 枚):

| 層 | 寄与したタイル |
|---|---|
| DEM5A | 6,531 枚(99.0%) |
| DEM10B | 6,094 枚(92.4%) |
| DEM5B | 678 枚(10.3%) |
| DEM5C | 248 枚(3.8%) |
| どの層にも無い(陸地なし) | 30 枚(0.5%) |

**二層以上を重ねたタイルは 6,213 枚(94.2%)。** 上位層だけで済んだタイルはごく一部で、
「最初に見つかった層で打ち切る」実装は大多数のタイルで穴を残していた。

合成前後の比較(母集団 2,805 件・実測 2026-09-08):

| 事項 | 合成前 | 合成後 |
|---|---|---|
| 欠測画素をまったく含まないレコード | 91 件 | **2,562 件** |
| 欠測画素を含むレコード | 2,714 件 | 243 件 |
| 欠測率の中央値 | 1.07% | **0.00%** |
| 欠測 50% 以上のレコード | 59 件 | 42 件 |
| 標高が取れなかったレコード | 数件 | **1 件** |

残る欠測 50% 以上の 42 件は島嶼・海岸の古墳である(舞子島古墳群・黒島古墳・
櫃石島古墳群・大戸鼻古墳群など)。半径 1,000 m の窓の大半が水面になるので、
これは欠陥ではなく**地形そのもの**である。

標高が取れなかった 1 件は **猫山古墳(山口県山口市深溝)** で、窓の 49.6% が水面、
中心画素も水面に当たる。`null` のまま残し、`data-manifest.json` に件数を出す。

### 実データの分布(実測 2026-09-08・母集団 2,805 件)

- 標高: 中央値 56.9 m / 範囲 −0.5〜1,084.3 m
- 傾斜: 中央値 6.5° / 範囲 0.0〜53.0°

**古墳の立地としてもっともらしいことは、正しさの証拠ではない。**
値の正しさは (a) 復号を富士山剣ヶ峰で検算する G-06、(b) 算出器を平面の解析解で
検算する T-030〜T-034、の二つが受け持つ。

### 3.6 源の内側にも同じ古墳の重複がある

名寄せは源と源のあいだの作業だと考えていたが、**Geoshape の内側にも同一古墳の重複行がある**
(実測 2026-09-08・母集団は `分類3 = 古墳` の 2,808 行)。

| 名称 | 所在 | Geoshape id | 備考 |
|---|---|---|---|
| 富士山古墳 | 栃木県 下都賀郡壬生町 大字七ツ石 | `090000035500` / `090000195000` | 名称・住所・座標が完全一致 |
| 狐塚古墳 | 山梨県 笛吹市 春日居町鎮目 | `190000048700` / `190000123900` | 同上 |
| 芝古墳群 | 京都府 長岡京市 井ノ内 | `260000086700` / `270000451900` | 座標が数 m 違い。県コードも 26 と 27 で食い違う(§3.2 と同じ破れ) |

したがって正規化は「1 行 = 1 レコード」ではなく、**同一内部 ID の候補を統合する**。
統合元の Geoshape id はすべて `raw_extra.geoshape_duplicate_ids` に残し、1 件も落とさない。

**3 組 6 行 → 3 レコードとなり、出荷候補は 2,808 → 2,805 件**(実測 2026-09-08)。

この破れは、HC-075 に従って「内部 ID が衝突したら落ちる」検算を実装の中に置いていたので、
実データを通した瞬間に例外で止まった。置いていなければ後勝ちで 3 件が黙って消え、
**件数 2,805 を誰も疑わなかった**。ハーネス側の一般化は HC-223 に起票した。

### 3.7 V1.0 の埋め込みは「立地」の埋め込みである

構想書 §16.2 は埋め込みの入力に墳形・墳丘長・時期・埋葬施設・出土品・文化財指定を挙げるが、
**実際に値があるのは地形と都道府県だけである**(実測 2026-09-09・母集団 2,805 件)。

| 入力 | 値がある割合 |
|---|---|
| `terrain.*`(標高・傾斜・起伏ほか) | 100.0% |
| `prefecture` | 100.0% |
| `mound.type` / `mound.length_m` / `chronology.year_min` | **0.0%** |
| `burial.*` / `artifacts.*` / `heritage.designation` | **0.0%** |

名称から墳形を拾えるかも測った —— 「前方後円」を含む名称は 6 件(0.2%)、
「円墳」8 件、「方墳」5 件で、**事実上ゼロ**である。
一方「群」を含む名称は 992 件(35.4%)あり、これは使える。

したがって **V1.0 の埋め込みは「その古墳が置かれた場所の埋め込み」である。**
UI では「AI 特徴類似度」ではなく **「立地の類似度」** と表記する。
文化庁 / Wikidata のスナップショットが置かれたら入力を足し、そのとき表記を見直す。

### 3.8 入力・潜在次元・前処理の置き場所

- **都道府県を類似度の入力に入れない。** 構想書 §19.3 の意図(「同じ地域だから似ている」
  というモデルにしない)を、重みを下げるのではなく**入れない**ことで満たす。
  地域は絞り込みの条件として別に置く
- **潜在次元は 64 ではなく 8 にする。** 入力が 15 次元しかないのに 64 次元の潜在を置くのは
  圧縮ではなく拡張であり、そこでの余弦類似度は入力空間の回転にすぎない
- **前処理(標準化)を ONNX の中に入れる。** `preprocessing.json` を読んで
  TypeScript 側でも同じ標準化を書くと、二つの実装がずれても検査は緑のままになる
  (HC-065 の「片方が出荷物でもう片方がテスト専用」の型)。境界を減らす

### 3.9 目玉を測る規則(実測の**前**に宣言する)

オートエンコーダを使うと言う以上、**それが標準化素性の素朴な余弦類似度で
代替されないこと**を示さなければならない(HC-064)。判定は次のとおり。
この規則は測定より先に書いており、結果に合わせて書き換えない。

| ID | 測るもの | 落ちたときにすること | 結果(実測 2026-09-09) |
|---|---|---|---|
| G-20 | 全 2,805 件について、AE 埋め込みの Top-20 近傍と、標準化素性の余弦類似度の Top-20 近傍の Jaccard 重なり(平均) | **0.90 以上なら「AE は素朴な余弦類似度で代替される」と結論する。** SPEC と UI から「AI が学習した埋め込み」という主張を降ろし、次元圧縮として説明し直す | **0.331 — 通過** |
| G-21 | 同じ潜在次元 8 の PCA と AE の再構成 MSE | **AE が PCA より悪ければ「非線形性が効いていない」と結論する。** 同上 | **AE 0.04489 / PCA 0.04966 — 通過** |

**両方とも通過したので、埋め込みは「学習した」と言ってよい。**
ただし言えるのはそこまでで、「よい埋め込みである」ことは示していない ——
素朴な余弦類似度と**違う**近傍を返すこと、同次元の線形圧縮より**再構成がよい**ことの二つだけである。
近傍が考古学的に妥当かどうかは、照合できる外部の正解が無いので測っていない(未測定)。

学習は 300 エポックの上限で止まった(早期打ち切りが働かず、検証損失はまだ下がっていた)。
これは「もっと学習できた」という意味であり、収束の証拠ではない。

**再現性(G-22)は実測で確認した**(2026-09-09〜10)。

- seed 42 で二度学習した埋め込みは **sha256 まで完全一致**(`a8f7d81a…`)
- seed 7 では検証 MSE 0.0401 / 再構成 MSE 0.0392 / Jaccard 0.3482 と**変わる**ので、
  一致が seed 固定の結果であることが言える
- seed 7 でも G-20 / G-21 は通過する(0.3482 < 0.90、0.0392 < 0.0497)ので、
  この結論は seed の当たり外れではない

seed を変えた出力は**同梱しない**(学習に約 10 分かかり CI では回せない)。
検査は手元でだけ走り、無ければ skip する。

### 3.10 二次元配置とクラスタの選定規則(実測の**前**に宣言する)

構想書 §18 は UMAP と HDBSCAN を指定する。UMAP は Python 3.14 でも入る
(`llvmlite` 0.49.0 に cp314 wheel がある)が、`numba` / `llvmlite` /
`pynndescent` / `tqdm` の 4 つを足すことになる。**足す価値があるかを測って決める。**
配るのは座標の JSON なので、これは**ビルド時だけの依存**であり実行時には出ない。

| ID | 測るもの | 判定 |
|---|---|---|
| G-23 | HDBSCAN のクラスタが偶然より構造を持つか。silhouette(cosine)を、**同じ大きさ分布のラベル置換 1,000 回**の帰無分布と比べる。あわせてノイズ率を測る | 観測値が帰無分布の 99 パーセンタイル以下、**または**ノイズ率 50% 以上なら「配れるクラスタ構造は無い」と結論し、クラスタ機能を落とす |
| G-24 | 二次元配置が埋め込みの近傍を保存するか。trustworthiness(k=20)を PCA-2 / t-SNE / UMAP で比べる | **UMAP が PCA-2 を +0.02 以上上回らなければ、依存を増やさず PCA-2 を採る。** 構想書 §18.2 の指定より実測を優先する |
| G-25 | 二次元配置の陽性対照 | 座標を無作為に並べ替えた配置の trustworthiness が、採用した配置より**明らかに低い**こと。低くなければ、この指標は何も測っていない |

**クラスタ番号に歴史的・考古学的な意味を自動で与えない**(構想書 §18.3)。

### 3.11 実測の結果(2026-09-10)

#### G-23 は**不通過**。クラスタ機能は落とす

| `min_cluster_size` | クラスタ | 大きさ | ノイズ率 |
|---|---|---|---|
| 20 | 2 個 | 697 / 46 | 73.5% |
| 30 | 2 個 | 149 / 61 | **92.5%** |
| 50 | 0 個 | — | 100% |

宣言した閾値(ノイズ率 50% 未満)をどの設定でも満たさない。
**閾値を動かさず、別の手法を後から持ち出さず、クラスタ機能を落とす。**

**直す前は通っていた。** 座標を共有するレコード(181 件・6.5%)が
`log1p(0) = 0` の縮退した山を作り、それが人工的にきれいな大クラスタ
(2,442 件・ノイズ 10.4%・silhouette 0.4600)になっていた。
ラベル置換検定は帰無の 99 パーセンタイルが 0.0603 で、余裕で「有意」だった ——
**縮退は本物の構造なので、統計的な対照は通してしまう**(HC-259)。
気づいたのは、各群が何で特徴づけられるかを特徴の中央値で読んだときだけである。

現れた 2 群自体は言語化できるので、**全体の分割ではなく「両極」**として記録する
(あわせて 210 件・全体の 7.5%)。

- **C0(149 件)**: 標高 21.8 m・傾斜 1.14 度・TRI 0.59・開放度 0.51 —— 低く平らな土地
- **C1(61 件)**: 相対標高 +12.3 m・傾斜 10.9 度・**開放度 0.913** —— 周囲から突き出た小高い場所

残る 92.5% は連続していて、群に切れない。**それが実測の結果である。**

#### G-24 / G-25 は通過。配置は t-SNE を採る

| 手法 | trustworthiness(k=20) | 所要 | 追加依存 |
|---|---|---|---|
| PCA-2 | 0.8250 | 0.1 秒 | なし |
| **t-SNE** | **0.9834** | 65.6 秒 | なし(sklearn 同梱) |
| UMAP | 0.9556 | 146.8 秒 | numba / llvmlite / pynndescent / tqdm |

陽性対照(座標を無作為に並べ替えた配置)は 0.5020 で、採用値 0.9834 から明確に離れる。

**宣言した規則はこの結果を覆っていなかった** —— G-24 は「UMAP が PCA-2 を
+0.02 以上上回らなければ PCA-2」としか書いておらず、t-SNE が UMAP に勝つ場合の
判断を定めていなかった(UMAP は PCA に +0.1306 で勝っている)。
規則の前文に書いた原則(**依存を足す価値があるかを測って決める**)に照らし、
宣言した指標で UMAP に勝ち、追加依存ゼロで 2 倍速い t-SNE を採る。
**規則を後から拡張したことをここに明記する。宣言した表は書き換えず残す。**

trustworthiness が測るのは**局所の近傍保存だけ**である。大域の構造を
どれだけ保つかは測っていないので、その点について主張しない。

### 3.12 地図の配り方と類似検索の規則(実装の**前**に宣言する)

**配る形式は GeoJSON にする(構想書 §32 の PMTiles から変える)。** 実測(2026-09-14・2,805 件)で、
一覧に要る欄(id・名称・県・市区町村)だけの GeoJSON は 528,004 バイト・**gzip 81,899 バイト**。
PMTiles の利点はタイル単位の部分読込だが、全件で 82 KB の点群では効かない。しかも
tippecanoe / pmtiles の CLI はこの環境に無く、Vercel での Range 配信という境界を一つ増やす。
**判定の見直し条件**: 地図ページの初回描画が実ブラウザで 3 秒を超えたら(G-27)、形式を見直す。

**「網羅しない」の具体例。** 最も著名な古墳の一つである**箸墓古墳(奈良県桜井市)は、
出荷データに 1 件も載っていない**(2026-09-14 確認: 母集団 2,805 件のうち名前に「箸」を含む
古墳 0 件・桜井市の古墳は 27 件)。Geoshape が収録するのは『日本歴史地名大系』の見出し項目で、
考古学上の重要度で選ばれた一覧ではない。地図ページの但し書き「網羅するものではありません」は
この種の欠落を指している。

**詳細は ID の先頭 2 文字で分割して、押したときだけ読む**(構想書 §33)。全件を一つにすると
5.8 MB(gzip 340 KB)になり、一覧を出すだけの人にも配ることになる。

**maplibre-gl は 5.24.0 に固定する。** 6.9.0 は公開から 5 日で、フリートで地図を出している
3 本はどれも 5 系で本番に出ている。新しい版の不具合を踏む理由が無い。

**類似検索は TypeScript と Python の二実装で照合する(G-26)。** 同点の扱いで食い違わないよう、
**並べる規則を両方で同じにする**: 類似度(内積)の降順、同点なら**レコードの並び順の昇順**。
配る埋め込みは小数 6 桁に丸めてあるので同点は実際に起こりうる(HC-073)。
結論(上位 10 件の ID)だけでなく**経路(類似度の値)も比べる**(HC-065)。

| ID | 測るもの | 判定 |
|---|---|---|
| G-26 | 全 2,805 件について、TypeScript 実装の上位 10 件が Python 実装と ID・順序とも一致し、類似度の差が `1e-9` 以下 | 1 件でも食い違えば落とす。**陽性対照**として、自分自身を除かない実装・類似度の昇順で並べる実装・同点規則を逆にした実装が落ちること。同点規則の対照が撃つには上位 10 件の中に同点が実在しなければならないので、**その存在も表明する**(実測 2026-09-14: 隣り合う同点 467 組) |

**宣言の訂正(実測の前に行った)。** 最初は陽性対照に「ユークリッド距離の昇順で並べる実装」を
挙げていたが、配る埋め込みは L2 正規化してあり、単位ベクトルでは `‖a−b‖² = 2 − 2a·b` なので
**ユークリッド距離の昇順と内積の降順は同じ並びになる**。誤りの見本にならず、撃たない対照だった。
「類似度の昇順で並べる実装」へ差し替えた。

### 3.13 地図ページの実測結果(2026-09-14)

| ゲート | 結果 |
|---|---|
| G-26 二実装照合 | **全 2,805 件で ID・順序・類似度(差 1e-9 以下)が一致。** 上位 10 件の中の隣り合う同点は 467 組あり、同点規則を揃えなければ食い違っていた。変異体 3 つ(自分を除かない/昇順/同点逆順)はそれぞれ落ちる |
| G-27 描画までの時間 | 5 回の実行で **872〜1,418ms**(締切 20,000ms)。点を空にした陽性対照は 8,000ms 待って 0 のまま。§3.12 の見直し条件(3 秒超)に当たらないので **GeoJSON のまま**とする |
| G-28 描画数 | 全件を視野に入れると、描画された一意 ID の数がデータ件数 2,805 と一致。層を隠す陽性対照は 0 を検出 |

**配った後に見つかったことは、どれも検査が緑のまま通る型だった。**

- **強調が見えなかった。** 似た立地 10 件を強調しても、地図は対象へ zoom 10 で寄ったままで視野に 0 件
  (江別古墳群で実測)。立地で選ぶので似た古墳は全国に散る。対象と 10 件が収まる範囲へ寄せるよう直した
- **出典が読めなかった。** ページの暗色テーマの本文色が地図の出典欄に継承され、最小コントラスト **1.12**。
  操作部品の色を明示して **7.92** にした。MapLibre の CSS に同じ詳細度で負ける背景指定もあった
- **和文の中に空白が 17 箇所あった。** JSX の本文を行の途中で改行すると、改行と字下げが空白一つに畳まれる
- **検品器の側の誤りが 3 件あった。** (a) `querySourceFeatures` は視野内に読み込まれたタイルの分しか返さず、
  重複もする(実体 10 件を 16 件)。(b) コントラストの背景を祖先要素から合成し、不透明な暗色の `body` が
  地図の色を上書きしていた —— 海と陸で値が毎回同一だったのが徴候。(c) 既定の 5 秒の時間制限が、
  他の重い処理と並行したときに時間切れを起こした(制限を 1 秒に縮めて同じ型を再現して確かめた)

**網羅しないことの具体例として、箸墓古墳は収録されていない**(§3.12)。撮影スクリプトはその名前で
検索する前提で書かれていて落ちた —— 記憶から書いた固有値だった(HC-266)。

### 3.14 探索画面の規則(実装の**前**に宣言する・2026-09-14)

**塗りは線形でなく 5 つの分位クラスにする。** 線形に 7 段で切ると、最も件数の多い段に
標高 82.0%・傾斜 54.7%・500 m 起伏 57.0% が入り、大半が同じ色になる(開放度だけは 23.9%)。
実測・母集団は値のある 2,804 件。

- **境界の規則**: 値のある件を昇順に並べた `v[0..n-1]` について、第 k 境界(k = 1〜4)は `v[⌊k·n/5⌋]`
- **所属の規則**: 値 `x` のクラスは「`x ≥ b` を満たす境界 `b` の数」(境界ちょうどの値は上のクラス)。値なしはクラス無し
- 同点があるとクラスの件数は均等にならない。件数は測って凡例に出し、均等であるとは主張しない
- **ランプ**: 青の 5 段 `#256abf → #3987e5 → #6da7ec → #9ec5f4 → #cde2fb`(暗い面では小さい値ほど暗い)。
  順序配色の検証を暗い面 `#1b1815` で全合格・最暗段のコントラスト 3.28:1。11 段は隣接明度差が基準未満で不合格だった

**地域では塗り分けない。** 散布図は任意の二点が隣り合うので、見分けを保てる色は 3 色まで。
地域は**強調**で見せる —— 選んだ点は元の色のまま、選ばれていない点は灰 `#6f675c`。
強調色 `#c8813a` と灰の組は CVD ΔE 14.0・通常視 ΔE 17.9・コントラスト合格(灰の彩度不足は設計どおり)。

**印の大きさは重なりを測って決める。** 作図の規律は直径 8 px 以上を求めるが、2,805 点では重なりうる。
実際の描画の大きさで「中心間の距離が直径未満になる相手を持つ点の割合」を測り、
**半径 4 で 50% 以下ならそれを採る。超えるなら 3、次に 2 と下げ、最初に 50% 以下になった半径を採る。**
どれも超えるなら半径 2 を採り、重なりの実測値を画面に書く。

**平均や中央値は、配った座標と値から TypeScript で計算し、Python の独立実装と照合する。**

- 中央値: 値のある件を昇順に並べ、奇数個なら真ん中、偶数個なら真ん中二つの平均。値なしは除く
- 範囲選択: 二次元配置の正規化座標の矩形 `[x0, x1] × [y0, y1]` で、**辺ちょうどの点を含む**
- **タップ**(loop_008 で追加・実装の前に宣言): 押してから離すまでの移動が描画単位で x・y とも 2 未満なら範囲選択ではなくタップとする。
  離した位置から描画単位 14 以内の最寄り点(距離の二乗が最小、同じ距離なら番号の小さい方)の名前を**固定して**出し、
  14 以内に点が無い所をタップすると消す。マウスは従来どおり重ねると出し、ポインタが図の外へ出たら消す。
  **タッチとペンでは、指を離した直後に来る pointerleave で消さない**(消すと、出した名前が一瞬で消える)

| ID | 測るもの | 判定 |
|---|---|---|
| G-29 | 散布図に描いた各点の `cx` / `cy` が、配った正規化座標を公開したスケールで写した値と一致する | 差 0.01 以下(描画単位)。**陽性対照**として 1 点を 1 単位ずらすと検出する |
| G-30 | 範囲選択で選ばれる点の集合が、Python の独立実装と一致する(矩形 6 つ) | ID 集合が完全一致。**陽性対照**として、辺を含まない実装・x と y を入れ替えた実装が落ちる。辺ちょうどの点が照合表の矩形に実在することも表明する |
| G-31 | 都道府県ごとの件数と地形の中央値、分位の境界と各クラスの件数が、Python の独立実装と一致する | 差 1e-9 以下。**陽性対照**として、平均で代用する実装・偶数個で下の真ん中を採る実装・境界の添字を切り上げる実装が落ちる |
| G-27 | 実ブラウザで地図に点が描かれるまでの時間 | **固定の待ち時間を置かず、出るまで待って締切で判定する**(HC-245)。締切 20 秒で 0 件なら落とす。**陽性対照**として、点のデータを空に差し替えると締切いっぱい待っても 0 のままであること |
| G-28 | 地図に描かれた点を全件視野に入れたとき、描画された数がデータの件数と一致する | 二重描画・欠落を捕まえる。**陽性対照**として、点の層を隠すと検品器が 0 を検出すること |

### 3.15 探索画面の実測結果(2026-09-14)

| ゲート | 結果 |
|---|---|
| G-29 点の位置 | 2,805 点すべてで `cx` / `cy` が公開したスケールの写像と差 0.01 以下。1 点を 1 単位ずらす陽性対照を検出 |
| G-30 範囲選択 | 矩形 6 つ(選ばれる件数 2,805 / 0 / 347 / 18 / 131 / 41)で ID 集合が Python と完全一致。辺ちょうどの点は 8 件実在。辺を含まない変異体・x と y を入れ替えた変異体はそれぞれ落ちる。実ブラウザでもドラッグの件数が枠から独立に数えた件数と一致 |
| G-31 集計 | 46 都道府県(値が偶数個の県 10)で件数・中央値、分位の境界、クラスの件数が差 1e-9 以下で一致。変異体 3 つはそれぞれ落ちる |
| G-14 図の収まり | 散布図の要素がすべて `viewBox` に収まる。`getBBox` は変換前の矩形なので、変換(`transform`)が無いことを先に確かめている。枠外に文字を足す陽性対照を検出 |

分位クラスの件数(実測)。同点があるので 561 前後にばらつく。

| 地形量 | 境界 | 各クラスの件数 |
|---|---|---|
| 標高 | 20.21 / 41.39 / 76.11 / 142.85 m | 559 / 562 / 561 / 561 / 561 |
| 傾斜 | 1.376 / 4.169 / 8.914 / 15.844 度 | 560 / 561 / 561 / 561 / 561 |
| 周囲より高い度合い | 0.4375 / 0.5704 / 0.7204 / 0.8954 | 557 / 564 / 561 / 560 / 562 |

**印の半径は 2 になった。規則の最後の枝である。** 重なりの割合は半径 4 で 99.5%・3 で 97.5%・2 で 87.7% と、
どれも 50% を超えた。t-SNE は近所どうしを詰めて置くので、2,805 点では避けられない。
作図の規律の直径 8 px を下回る逸脱なので、実測値を図の注記にそのまま出している。
割合は描画単位(`viewBox` の 600)で測る。点の位置と半径は同じ倍率で拡大縮小されるので、
割合は表示の大きさに依らない。

**配った後に見つかったこと。**

- **和文の中に空白が 5 箇所あった。** 出典名を「国土地理院 標高タイル」のように組織名と名称を半角空白で区切った 4 箇所と、
  図の注記の 1 箇所。loop_005 で常設した検品器の検査が捕まえた。検品器は例を 3 件に切って出していたので、
  見えた 3 件だけを直して再び落ちた。報告に件数を出すよう直し、同じ行に書いた型はビルド前のテスト(T-069)でも拾う(HC-277)
- **検品器が読み込めずに落ちると、不合格と同じ終了コード 1 が出た。** 変数名を二重に宣言して検査は 1 つも走っていなかった。
  読み込みの失敗を 3 に写す起動器 `harness/smoke-run.mjs` を置き、`pnpm smoke` はそれを通す
- **テストが全部通っているのに vitest が終了コード 1 を返した。** ワーカーの応答の時間切れ(`Timeout calling "onTaskUpdate"`)が
  3 回続き、うち 1 回は分割鍵の照合(T-061)も既定の 5 秒を越えた。類似検索の陽性対照 3 本が全件を回し、1 本 13〜20 秒の
  同期処理になっていた。変異体は 1 件食い違えば足りるので最初の食い違いで止め、81〜95 秒が 52 秒・未処理エラー 0 になった。
  応答の時間切れの原因が同期処理の長さだという点は推定で、確かめたのは 1 回の緑までである(HC-278)

### 3.16 公開前の点検(L7・2026-09-15)

| 項目 | 結果 |
|---|---|
| G-13 資産の合計 | `public/` 3.66 MB・`out/` 5.78 MB(304 ファイル)。上限 60 MB |
| アクセシビリティ(T-071) | 6 ページすべてで、言語 `ja`・`h1` 1 つ・操作部品すべてに名前・図すべてに代替の説明・本文の文字が WCAG AA・Tab で焦点の輪が見える。陽性対照 4 つ(名前の無いボタン・読めない文字・説明の無い図・焦点の輪を消す指定)をそれぞれ拾う。地図の中の文字は測っていない(出典欄だけ別に測る) |
| 依存 | next を 15.5.25 に固定(15.5.2 系は CVE-2025-66478 で Vercel が配布を拒む) |

**公開の直前に見つかったこと。どれも手元の検査は緑だった。**

- **トップページの散文が古かった。** 見出し「いまの状態(L4)」、本文「二次元配置の探索画面はこれから作ります」、
  入口は地図だけ。数値は manifest から読むので正しく変わり、隣の散文だけが残った(HC-280 の型)。
  manifest の `stage` は成果物の有無から L4 で止まる設計で、`shipped` は false 固定だった。
  入口を 3 つにし、段階名を画面から外し、画面の散文から未来の約束と段階名を拾う T-073 を置いた
- **文書が実際と違っていた。** README と THIRD_PARTY_NOTICES が、取り込んでいない Wikidata・文化庁を使っているように書き、
  README は「ブラウザで ONNX 推論」(実際は配った埋め込みの内積)と「`/api/health` を除きサーバ関数を持たない」
  (実際は静的な JSON)と書いていた。出典のページも使わない源の役割に「(予定)」と書いていた
- **CI は一度も緑になりえなかった。** 新しい clone に `CI=true` で再現すると、T-055 が `sklearn` の import で落ち、
  G-08 の ONNX 照合 3 件は `onnxruntime` が無く skip で緑に見えていた。手元の venv には全部入っている。
  検査用の追加依存 `validate` を置き、CI では「モジュールが無い」を skip でなく失敗にした(T-074)

**公開(2026-09-15)。** 本番は https://kofun-atlas-ai.vercel.app 。GitHub `twill3c/kofun-atlas-ai`(public)。

| 項目 | 結果 |
|---|---|
| GitHub Actions の最初の実行 | run 34900988678 は **web ジョブが failure** —— `pnpm/action-setup` の `version: 10` と `package.json` の `packageManager` の二重指定。新しい clone での再現は `run:` の行しかなぞらず、action の入力検査は再現できなかった。直した run 34901238304 で 3 ジョブとも success。二重指定は T-075 で静的に拾う |
| Vercel | CLI の初回デプロイで GitHub 連携が自動で張られた。以後 `main` への push が本番に出る |
| 本番の検品(T-072 ほか) | データ 5 ファイルと 6 ページの描画本文が手元の `out/` と一致したうえで 170 件 OK |
| タッチ端末(loop_008) | app-menu の「スマホ対応」を名乗る前に本番へタッチ端末の探針を当てると 10/11。探索画面の散布図は点をタップしても名前が出なかった(ホバーが無く、`onPointerDown` が大きさ 0 の範囲選択を始め、指を離すと `pointerleave` で消していた)。§3.14 にタップの規則を足して直し(526150b)、本番の探針 11/11・検品器 175 件 OK。app-menu は `mobile: true` |
| `/api/health` | 初回は `application/octet-stream` で配られた(静的書き出しは拡張子の無いファイルに MIME を付けない)。`vercel.json` で JSON を付け、本番の検品に項目を足した。push(c9b8c1d)で配り直した本番で `application/json` を確かめ、本番の検品は 171 件 OK。CI run 34901728552 も 3 ジョブ success |

## 4. データ源と採用可否

| ID | 源 | 役割 | ライセンス / 条件 | V1.0 |
|---|---|---|---|---|
| `geoshape` | 『日本歴史地名大系』施設・地点項目データセット(CODH) | 位置・名称・読み・住所 | CC BY 4.0 / doi:10.20676/00000456 | **主データ** |
| `gsi_revgeo` | 国土地理院 逆ジオコーダ + 市区町村表 | 都道府県・市区町村(`muniCd`) | 国土地理院コンテンツ利用規約 | **採用**(結果を同梱) |
| `gsi_dem` | 国土地理院 標高タイル | 地形特徴(派生値のみ再配布) | 同上 | **採用** |
| `wikidata` | Wikidata | QID・別名・時期・外部 ID | CC0 | 採用(人手スナップショット) |
| `bunka` | 文化庁 国指定文化財等データベース | 指定文化財・解説・種別 | 文字情報は出典記載で利用可 | 採用(人手スナップショット) |
| `nabunken` | 全国遺跡報告総覧 / 文化財総覧 WebGIS | 文献への外部リンクのみ | 個別確認 | リンクのみ(取得しない) |
| `nara_hgis` | 奈良女子大『全国古墳データベース』 | — | 学術研究限定・改変/再配布禁止 | **不採用**(Sources に URL を記すのみ) |

`source_tier`(構想書 §3)は全レコードに持たせる。出荷データに入れてよいのは A〜D のみ。

## 5. 機能要求

| ID | 要求 | 優先度 | 着手ループ |
|---|---|---|---|
| F-01 | Geoshape から古墳候補を抽出し `KofunRecord` に正規化する | must | L0 |
| F-02 | 内部 ID を座標・名称から決定論的に採番し、外部 ID を `external_ids` に保持する | must | L0 |
| F-03 | JSON Schema による出荷データ検証を CI で回す | must | L0 |
| F-04 | 座標から都道府県・市区町村を確定する(逆ジオコーダ) | must | L1 |
| F-05 | Wikidata / 文化庁スナップショットを取り込み名寄せする | must | L1 |
| F-06 | 値ごとの出所(`provenance`)を保持する | must | L1 |
| F-07 | GSI DEM から地形特徴量を算出する | must | L2 |
| F-08 | 特徴行列を作り PyTorch オートエンコーダで 64 次元埋め込みを学習する | must | L3 |
| F-09 | ONNX へ書き出しブラウザ内で推論する | must | L3 |
| F-10 | 立地の似た古墳の検索(Top N) | must | L5 実装(地図ページ・上位 10 件。G-26 の二実装照合) |
| F-11 | 築造時期の参考推定(分類器)と較正の提示 | should | **保留**(教師ラベルが 0 件。§3.7) |
| F-12 | 二次元配置による探索 | should | L4 実装(t-SNE)。**クラスタは G-23 不通過で落とした**(§3.11) |
| F-13 | MapLibre の全国地図・都道府県の絞り込み・名称検索・詳細 | must | L5 実装。**PMTiles でなく GeoJSON**(§3.12・gzip 82 KB)。**時間スライダーは作らない** —— 築造時期が 0 件で、動かしても何も変わらない |
| F-14 | 古墳詳細パネル(公表値 / 派生値 / AI 値を視覚的に分ける) | must | L5 |
| F-15 | Explore(UMAP・類似)・Compare(地域比較)・Data Quality UI | should | L6 |
| F-16 | Methodology / Sources ページと帰属表示 | must | L6 |
| F-17 | フリート共通フッタ(MIT / GitHub / 歩き方 / 設計図 / App Menu) | must | L7 |

## 6. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 本番はサーバ関数・DB・cron を持たない静的配信(`/api/health` を除く) | `vercel.json` と Next.js のビルド出力を検査 |
| N-02 | 公開静的アセット合計 < 60 MB | ビルド後に実測して閾値検査 |
| N-03 | ONNX モデル合計 <= 10 MB | ファイルサイズ検査 |
| N-04 | 外部 API を本番から直接叩かない | ソース走査(陽性対照つき) |
| N-05 | API キー・秘密情報を持たない | `.env.example` のみ・secret scanning |
| N-06 | 日本語本文に別字種・制御文字が混入しない | `python harness/text_hygiene.py` |
| N-07 | 主要画面が 320 / 768 / 1280 px で崩れない | 実ブラウザ検品(複数幅) |
| N-08 | 個人情報を収集しない(アカウント・Cookie 必須機能・位置取得なし) | 実装レビュー |

## 7. 品質ゲート

**この表は宣言であって実装ではない**(HC-157)。各 `G-xx` は
TEST_SPEC のケースから ID で参照されるか、この表で「未実装」と明記されているかのどちらかでなければならない。
対応を機械で数える検査を `tests/validation/test_gate_coverage.py` に置く(G-16 自身がそれ)。

| ID | ゲート | 判定 | 状態 |
|---|---|---|---|
| G-01 | 出荷レコード件数 >= 2,000(L0 実測 2,805 件) | 件数 | L0 実装 |
| G-02 | 座標が lat 20–50 / lon 120–155 の外にある行 0 件・座標欠測率 <= 10% | 件数 | L0 実装 |
| G-03 | 全レコードが `sources` を 1 件以上持ち、`license` が空でない | 件数 | L0 実装 |
| G-04 | 内部 ID が一意で、`canonical_text` から再計算して一致する | 全件再計算 | L0 実装 |
| G-05 | `prefecture` / `municipality` が座標由来の `muniCd` と一致する。CSV の県名・県コードを出所とするレコードが 0 件。**陽性対照**として、逆ジオコーダが CSV の県コードを実際に覆した件が 1 件以上あること | 全件 | L1 実装 |
| G-06 | DEM 復号が富士山剣ヶ峰周辺 3×3 タイル(z15 / DEM5A)の最大標高で公表値 3776.12 m と ±2 m 以内に一致する | 実測 | L0 実装(実測 3774.48 m・差 1.64 m) |
| G-18 | 地形特徴が定義域に収まる(標高 −50〜3,800 m・傾斜 0〜90°・方位 0〜360°)。**半径を広げると局所起伏が減らない**(窓が包含関係にあるので定義から従う)。平坦地の方位は `null` で 0 度と混ぜない | 全件 | L2 実装 |
| G-19 | 層の合成が画素単位である。上位層の穴が下位層で埋まり、かつ上位層に値がある画素は下位層で上書きされない。**両方を対照で確かめる** | 全件 | L2 実装 |
| G-07 | 名寄せの false merge rate < 1%(ゴールドセット 100–300 件) | 実測 | **未実装**(L1) |
| G-08 | PyTorch と ONNX Runtime の出力差 `max_abs_error <= 1e-4`。**前処理も含めた同じ入力**で比べる | 実測 | L3 実装 |
| G-20 | AE 埋め込みの Top-20 近傍が、標準化素性の余弦類似度の Top-20 近傍と Jaccard 平均 0.90 未満で異なる(§3.9 の宣言に従う) | 実測 | L3 実装 |
| G-21 | AE の再構成 MSE が同じ潜在次元の PCA 以下(§3.9 の宣言に従う) | 実測 | L3 実装 |
| G-22 | 学習が再現する。同じ seed で二度学習して埋め込みが一致し、**seed を変えると変わる** | 実測 | L3 実装(sha256 まで一致) |
| G-23 | HDBSCAN のクラスタが偶然より構造を持ち、ノイズ率が 50% 未満(§3.10 の宣言に従う) | 実測 | **L4 で不通過 → クラスタ機能を落とした**(§3.11)。落ちたままであることを検査する |
| G-24 | 二次元配置の trustworthiness(k=20)が PCA-2 を上回る(§3.10 の宣言に従う) | 実測 | L4 実装(t-SNE 0.9796 / PCA-2 0.8115) |
| G-25 | 二次元配置の陽性対照。座標を並べ替えた配置が明確に低い | 実測 | L4 実装(0.5007) |
| G-26 | 類似検索の TypeScript 実装が、Python の照合表と全件で ID・順序・類似度まで一致する(§3.12) | 全件 | L5 実装 |
| G-27 | 実ブラウザで地図に点が締切 20 秒以内に描かれる。固定の待ち時間を置かない(§3.12) | 実測 | L5 実装(872〜1,418ms) |
| G-28 | 全件を視野に入れたとき、描画された点の数がデータ件数と一致する(§3.12) | 全件 | L5 実装 |
| G-29 | 散布図の各点の位置が、配った正規化座標を公開したスケールで写した値と差 0.01 以下で一致する(§3.14) | 全件 | L6 実装 |
| G-30 | 範囲選択で選ばれる点の集合が Python の独立実装と一致する(矩形 6 つ・§3.14) | 全件 | L6 実装 |
| G-31 | 都道府県ごとの件数と中央値、分位の境界と各クラスの件数が Python の独立実装と一致する(§3.14) | 全件 | L6 実装 |
| G-09 | 時期分類器の train/test が `municipality + 古墳群` で分離され、同一群が両側に現れない | 全件 | **未実装**(L4) |
| G-10 | 時期分類器を「参考推定」として出すのは macro F1 >= 0.60 かつ各クラス >= 100 件のときだけ。満たさなければ UI に「実験」と出す | 実測 | **未実装**(L4) |
| G-11 | 禁止ソース(奈良女子大 全国古墳DB)由来のデータが出荷物に 0 件。`sources` と `provenance` の**両方**を見る。**陽性対照**(混入させた偽レコードを検査が落とすこと)を二経路に置く | 件数 | L1 実装 |
| G-12 | `harness/text_hygiene.py` の違反 0 件 | 件数 | L0 実装 |
| G-13 | 公開静的アセット合計 < 60 MB | 実測 | L7 実装(`public/` は T-070、`out/` はビルド後の検品器。2026-09-15 実測 public/ 3.66 MB・out/ 5.78 MB) |
| G-14 | 出力する図の `text` / `path` / `rect` の境界矩形が `viewBox` に収まる(HC-159) | 全要素 | L6 実装(`harness/smoke.mjs` の探索画面。陽性対照: 枠外に文字を足すと検出) |
| G-15 | 図・地図を触ったループは実ブラウザで 320 / 768 / 1280 px の三幅を実際に見る | 手順 | L5 実装(`harness/_shot_widths.mjs` で 320 / 768 / 1280 px を撮って目視。`harness/smoke.mjs` が 320 / 360 / 768 / 1280 / 1680 px で溢れを測る。最初は 360 px までしか測っても見てもおらず、指定の最小幅を後から足した) |
| G-16 | すべての `G-xx` がテストから ID 参照されるか、本表で「未実装」と明記されている | 全件 | L0 実装 |
| G-17 | `chronology.source_kind = ai` の値が `published` の値を上書きしているレコードが 0 件 | 件数 | **未実装**(L4) |

## 8. データモデル

構想書 §7.1 の `KofunRecord` を踏襲する。V1.0 の変更点は次の四つだけ:

1. `prefecture` / `municipality` / `muni_cd` は逆ジオコーダ由来(§3.2)
2. `raw_extra` に、採用しなかった元の県名・県コードと、統合した重複行の id を退避する
3. `provenance` は構想書 §13 のとおり値ごとに持つ
4. `prefecture` / `municipality` は **キーは必須だが値は `null` を許す**。
   L0 では未確定だからである。`null` を許さないと L0 の出力が表現できず、
   逆に「とりあえず何か入れる」と §3.2 で捨てたはずの値が復活する。
   **非 null を要求するのは出荷の条件(G-05)であって、スキーマの条件ではない**

共有コードは `kofun_atlas/` パッケージに置き、`scripts/` と `ml/` は
それを呼ぶ薄い入口にする(構想書 §6 は `scripts/*.py` を独立実行体としているが、
探索で書いた規則を実装へ「写す」経路を作らないため一箇所に集める — HC-069)。

内部 ID(構想書 §10):

```text
canonical_text = normalized_name + "|" + prefecture + "|" + municipality
                 + "|" + round(lat, 4) + "|" + round(lon, 4)
id = "kofun_" + sha1(canonical_text)[0:10]
```

`prefecture` / `municipality` が逆ジオコーダ由来になるため、**ID は L1 で確定する**。
L0 の時点では逆ジオコーダを通す前の暫定 ID になるので、
`data/interim/` に置き、出荷対象にしない。

## 9. ループ計画

| ループ | 主題 | 主な成果物 |
|---|---|---|
| L0 | 立ち上げ・源の実測・Geoshape 取り込み | SPEC / TEST_SPEC / スキーマ / 抽出・正規化 / DEM 復号器 / Next.js 骨格 |
| L1 | 逆ジオコーディングによる行政区画の確定・ID 確定・出所追跡・禁止ソース検査 | `data/processed/kofun.json` / `data/raw/gsi/revgeo.jsonl` |
| L2 | 地形特徴 ETL(GSI DEM) | `terrain.*` / `data/derived/terrain.jsonl` / DEM 種別の実測 |
| L3 | 立地の特徴行列・オートエンコーダ・ONNX 書き出し | `kofun_encoder.onnx` / `embeddings.json` / model card |
| L4 | 二次元配置(クラスタは G-23 不通過で落とした) | `projection.json` |

**Wikidata / 文化庁の取り込みと名寄せ(F-05・G-07)は、スナップショットが置かれた時点で
差し込むループとして扱う。** どちらも人手で取得する方式にしたため(§3.3 / §3.4)、
自動では進められない。照合相手が無い状態でゴールドセットは作れないので、
`G-07` は「未実装」のまま据え置く。

**築造時期の推定(F-11・G-09・G-10・G-17)も同じ理由で保留する。**
構想書 §17.1 は「公刊・公開ソースから年代を取得できる古墳だけを教師データにする」と
定めているが、その年代が **1 件も無い**(実測 2,805 件中 0 件)。
教師データが 0 件では分類器を作れないし、作ってはならない ——
`chronology` を AI 推定で埋めれば、公表値との区別が最初から失われる。
| L5 | 地図 UI(MapLibre / GeoJSON / 都道府県の絞り込み / 名称検索 / 詳細 / 立地の似た古墳) | `/map` |
| L6 | Explore / Compare / Data Quality / Methodology / Sources | `/explore` `/compare` `/methodology` `/sources` |
| L7 | QA・アクセシビリティ・性能・公開 | Vercel 本番 / README / THIRD_PARTY_NOTICES |

## 10. スコープ外(GEN-SCOPE の予防線)

- 全国約 16 万基の完全収録。**採録範囲を画面に明示し「全国網羅」と誤読させない**
- 未発見古墳の探索・精密候補座標の公開
- 発掘調査報告書 PDF の無断再配布・本文学習
- 奈良女子大『全国古墳データベース』のデータ再配布
- AI による歴史的事実・政治勢力・王権帰属の断定
- 画像の転載(V1.0 は画像なしで成立させる)
- Vercel 上での学習・GPU 推論 API
- ユーザーアカウント・投稿・課金
