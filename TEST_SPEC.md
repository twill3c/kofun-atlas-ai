# TEST_SPEC.md — kofun-atlas-ai

<!-- scaffold template v1.30.0 から展開(2026-09-08) -->

## 実行規約

- `.venv/Scripts/python.exe -m pytest -x -q` を stage 3–5 の判定に使用。
  マーカー: `unit` / `integration` / `validation`
- `integration` は外部ネットワークに触れる。既定では実行せず、
  `-m "not integration"` を CI と通常判定に使う。ネットワーク検査は
  `KOFUN_ALLOW_NETWORK=1` を立てたときだけ走る
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**し、
  導出過程をコメントに残す

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威 | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | 実測日と実測値、および**母集団**をコメントに残す |

件数・行数は定数で書かず、集合の一致・取りこぼしの不在という不変量で書く。
ただし**外部権威が公表している値**(富士山剣ヶ峰の標高など)は定数でよい。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `tests/fixtures/dem5a_15_29011_12939.png` | 国土地理院 標高タイル DEM5A z15(2026-09-08 取得) | **非循環**。復号結果の最大標高を、国土地理院が公表する剣ヶ峰標高 3776.12 m と突き合わせる |
| `data/raw/geoshape/nrct-poi-20250515.csv`(同梱の実データ) | Geoshape / CODH | 陰性対照。誤検出 0 を**作った例ではなく実データの正常な部分**で確かめる(HC-074) |
| 合成 PNG(テスト内で構成) | 構想書 §14.2 の符号規則から直接組む | 符号・NA 分岐の網羅。**実装を使わずに作る**ので恒等式にならない |
| 合成タイル(テスト内で構成) | 全体画素座標の平面を焼き込む | モザイク結合の位置合わせ。1 画素ずれ・転置を陽性対照で落とす |
| 逆ジオコーダ判定 30 件(seed 20260908) | 国土地理院 逆ジオコーダ | 非循環。県の真値を Geoshape 外から取る(L1 で使用) |
| 沖縄県 0 件(L1) | 古墳時代の文化圏が琉球列島に及ばないという外部の事実 | 非循環。**この事実は逆ジオコーディングの入力に使っていない** |

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 / G-01 | 実 CSV から候補抽出する | `分類3 == "古墳"` の行だけが選ばれ、件数 >= 2,000 |
| T-002 | F-01 | 候補集合の性質(SPEC §3.1 の根拠) | 名称に「古墳」を含むが `分類3 != 古墳` の行が 0 件。かつ `分類3 == 古墳` で名称に「古墳」を含まない行が 1 件以上ある(= 名称規則では取りこぼす) |
| T-003 | F-01 | 除外分類が候補に混ざらない | 貝塚 / 天皇陵 / 横穴群 の行が候補に 0 件。**かつ各分類が CSV に実在する**(走査対象が空でないことの確認) |
| T-004 | F-02 / G-04 | 内部 ID の決定論性 | 同じ入力から同じ ID。`canonical_text` を再構成して再計算すると全件一致。全候補で ID 重複 0 |
| T-005 | F-02 / G-04 | ID の陽性対照 | 座標を 5 桁目だけ変えた 2 件は**同じ** ID(round 4 桁)、4 桁目を変えた 2 件は**異なる** ID |
| T-006 | G-02 | 座標の範囲 | 全候補が lat 20–50 / lon 120–155 に入る。範囲外を混ぜた合成行は落ちる(陽性対照) |
| T-007 | F-03 | JSON Schema 検証 | 正規化済みレコードが `schemas/kofun.schema.json` に適合。必須欄を欠いた合成レコードは落ちる(陽性対照) |
| T-008 | G-03 | 出典の存在 | 全レコードが `sources` を 1 件以上持ち `license` が空でない。空 license の合成レコードは落ちる(陽性対照) |
| T-009 | F-07 / G-06 | DEM 復号の非循環オラクル | フィクスチャタイルの最大標高が、国土地理院公表の剣ヶ峰標高 3776.12 m と ±2 m 以内 |
| T-010 | F-07 | DEM 復号の符号・NA 分岐 | 合成 PNG で `x < 2^23 → h = 0.01x` / `x == 2^23 → None` / `x > 2^23 → h = 0.01(x − 2^24)` |
| T-011 | F-07 | DEM 復号の陽性対照 | R と B を入れ替えた復号器はフィクスチャで ±2 m 以内に入らない(= このオラクルは実際に撃つ) |
| T-012 | F-07 | タイル座標変換 | `lonlat_to_tile` が既知値(赤道・本初子午線 z0/z1、および実測した富士山 z15 タイル)と一致 |
| T-013 | F-07 | 窓に必要なタイル集合 | ±1000 m 窓のタイル集合が、窓の四隅を含み、かつ窓外のタイルを含まない |
| T-014 | G-12 | 文字衛生 | `harness/text_hygiene.py` の違反 0 件 |
| T-015 | G-16 | ゲート対応 | SPEC の全 `G-xx` が、この TEST_SPEC のケース表から参照されるか SPEC で「未実装」と明記されている。**どちらでもないゲートがあれば落ちる** |
| T-016 | F-01 / G-04 | 源内の重複行が統合される(SPEC §3.6) | 同一内部 ID になった候補が 1 レコードへ統合され、元の Geoshape id が全て `raw_extra.geoshape_duplicate_ids` に残る。**統合が実際に起きた証拠として、統合された組が 1 組以上ある**(起きていなければこの経路は何も検査していない — HC-071) |
| T-017 | F-01 | 統合の陽性対照 | 名称の違う 2 件は統合されない。座標が 4 桁で一致し名称も一致する合成 2 件は統合される |
| T-018 | F-04 | `muniCd` の名称解決 | 国土地理院 `muni.js` の表が `muniCd` を県コード・県名・市区町村名へ一意に写す。先頭ゼロの有無で引けなくならない（`01217` と `1217` が同じ項目を指す） |
| T-019 | F-04 | 逆ジオコーダ応答の解釈 | `{"results":{"muniCd":…,"lv01Nm":…}}` は値を返し、**`{}` は「陸上でない」という答え**として `(None, None)` になる。`{}` を障害として扱わない |
| T-020 | F-04 / G-05 | 県・市区町村の出所 | 出荷レコードの `prefecture` / `municipality` が `muni_cd` から導いた値と一致する。`raw_extra` の県名・県コードを出所とするレコードが 0 件 |
| T-021 | G-05 | G-05 の陽性対照 | **`muniCd` 由来の県と Geoshape の県コードが食い違うレコードが実際に存在する**。0 件なら逆ジオコーディングは何も変えておらず、この経路は検査になっていない（HC-071） |
| T-022 | F-02 / G-04 | 確定 ID | 県・市区町村を含めた `canonical_text` から再計算して全件一致し、重複 0 |
| T-023 | F-06 | 出所追跡 | `prefecture` / `municipality` の `provenance` が `gsi_revgeo` を指し、`location` は `geoshape` を指す。値と出所の対応が入れ替わっていない |
| T-024 | G-11 | 禁止ソース混入 | 出荷レコードの `sources` に禁止ソース（`nara_hgis`）が 0 件。**陽性対照**として、混入させた合成レコードを検査が落とす |
| T-025 | G-05 | 陸上でない点の扱い | `muni_cd` が付かなかったレコードは `prefecture` が `null` のまま残り、件数が `data-manifest.json` に出る（黙って捨てない） |
| T-026 | F-04 / F-06 | `apply_geocode` 単体 | 合成レコード 1 件で、(a) 解決できた場合は県・市区町村・`muni_cd`・出所が入り ID が振り直される、(b) 解決できなかった場合は `null` のまま `manual_review` になり ID が変わらない。**実データを使わずに両分岐を通す** |
| T-028 | G-05 | 逆ジオコーディングの健全性 | 逆ジオコーダが返した県が、CSV の**二つの県欄（県コード / 住所の県名）の少なくとも一方**と一致する。両方と食い違うレコードが 0 件（実測 2026-09-08・2,805 件）。逆ジオコーダが体系的に狂えばここが崩れる。**CSV の県欄は逆ジオコーダの入力ではないので循環しない** |
| T-029 | G-05 | 誤りの内訳 | 県コードが誤ったレコードが多数（実測 1,569 件）、住所の県名が誤ったレコードが少数（実測 1 件）あり、**どちらも 0 件ではない**。片方だけを採る実装に戻したら落ちる |
| T-030 | F-07 | 傾斜・方位の解析解 | 傾き既知の**平面** `h = a·x + b·y + c` に対し、Horn 法が `slope = atan(√(a²+b²))` と方位を厳密に返す（平面では有限差分が厳密になる）。8 方位で確かめる |
| T-031 | F-07 | 傾斜の陽性対照 | 画素サイズを取り違えた実装・x と y を入れ替えた実装が T-030 に落ちる。落ちなければ T-030 は何も検査していない |
| T-032 | F-07 | 平坦地 | 完全な水平面で `slope = 0`、方位は `null`（0 度＝北と区別する） |
| T-033 | F-07 | 局所起伏の解析解 | 平面上の半径 r の円窓で `local_relief = 2·r·√(a²+b²)`（対角の 2 点が最大・最小になる）。相対標高は対称性から厳密に 0 |
| T-034 | F-07 | TRI の解析解 | 平面 `h = aX + bY + c` 上で Riley の TRI が **`mpp·√6·√(a²+b²)`** と厳密に一致する。導出（8 近傍との高度差の二乗和 = `6·mpp²(a²+b²)`）をテスト内で近傍ごとに検算してから使う |
| T-035 | F-07 | 欠測の扱い | NoData を含む窓でも算出が落ちず、`null` と欠測率が残る。**0 で埋めない**（構想書 §14.5）。全欠測の窓は全特徴が `null` |
| T-039 | F-07 | 窓のモザイク結合 | **全体画素座標の平面**を焼き込んだ合成タイルを並べ、組み立てた窓が解析解と厳密に一致する。1 画素ずれ・行列の入れ替え・タイル境界の重複がすべて落ちる。**どの層にも無いタイルは NaN の板**として入り、そこだけが欠測になる |
| T-041 | F-07 / G-19 | 層の合成は画素単位 | 上位層に穴（NoData）があり下位層に値があるとき、合成後の格子に穴が残らない。**「タイルが存在する ⇒ 値がある」は偽**で、DEM5A はタイルがあっても大半が NoData のことがある（実測: 石馬谷古墳の中心タイルは DEM5A が 82.1% 欠測・中心画素 NaN、DEM10B は欠測 0%・29.17 m） |
| T-042 | F-07 / G-19 | 合成の陽性対照 | 上位層だけを使う実装では穴が残る。かつ**上位層に値がある画素は下位層で上書きされない**（優先順が効いている）。どちらも確かめないと、合成しているのか単に下位層を使っているのか区別できない |
| T-040 | F-07 | 窓の形 | 窓が中心画素を持つ奇数辺で、半径 `r` を覆う最小の大きさである。中心画素が対象点の含まれる画素と一致する |
| T-043 | F-07 / G-18 | 丸めが値域を外さない | 方位角を丸めても半開区間 `[0, 360)` に留まる。`359.9996 → 360.0` を `0.0` に戻す。**丸めは値域の外へ押し出す**ので、丸めた後の値で確かめる（実データ 1 件がスキーマ検証で捕まった） |
| T-038 | F-07 | 開放度プロキシ | 平面上では中心より低い画素の割合がちょうど半分に近い（`0.45 ≤ p ≤ 0.50`、等高の画素があるぶん 0.5 未満）。これは**本アプリ定義の代理指標**であり標準的な openness ではない |
| T-036 | F-07 / G-18 | 実データの分布 | 全 2,805 件の標高が日本の陸上の範囲（−50〜3,800 m）に入り、傾斜が 0〜90 度、方位が 0〜360 度に入る。**範囲外 0 件**。半径を広げると局所起伏が減らない。平坦地の方位は `null` |
| T-037 | F-07 | 二解像度の一致 | 標本 40 件で z14 と z15 の中心標高の差の中央値が 1 m 以内（同じ地点を別解像度で測って食い違わない） |
| T-044 | F-08 | 特徴行列の形と欠測 | 行数がレコード数と一致し、列名が固定順。**欠測は 0 で埋めず、値は学習データの中央値で補い、同時に `is_missing` 列を立てる**（構想書 §19.2）。補った値が実際に中央値と一致する |
| T-045 | F-08 | 標準化の閉形式 | 中央値と IQR で標準化する。既知の分布（等差数列）で `median` と `IQR` を手計算し、変換後の値と厳密に一致することを確かめる。IQR が 0 の列は 1 として扱い、`nan` を生まない |
| T-046 | F-08 | 方位の循環符号化 | 方位を `sin/cos` で入れる。359° と 1° の距離が 179° と 181° の距離より小さい。方位が `null` のとき両成分 0 かつマスクが立つ |
| T-047 | F-09 / G-08 | ONNX 一致 | 全 2,805 件で PyTorch と ONNX Runtime の埋め込みの `max_abs_error <= 1e-4`。**前処理を含む同じ生の入力**を両方へ渡す（前処理は ONNX の中にある） |
| T-048 | F-09 | ONNX の陽性対照 | 入力を変えると出力が変わる（定数を返していない）。特徴を 1 列だけ動かした入力で埋め込みが動く |
| T-049 | F-08 / G-22 | 再現性 | 同じ seed で二度学習した埋め込みが一致する。**seed を変えると変わる**ことも確かめる（一致が seed 固定の結果であることの確認） |
| T-050 | F-10 / G-20 | 目玉の対照（代替性） | AE 埋め込みの Top-20 近傍と、標準化素性の余弦類似度の Top-20 近傍の Jaccard 平均が **0.90 未満**。SPEC §3.9 で測定前に宣言した規則に従う |
| T-051 | F-08 / G-21 | 目玉の対照（非線形性） | AE の再構成 MSE が同じ潜在次元 8 の PCA 以下。**PCA の再構成を独立に計算**して比べる |
| T-052 | F-08 | 同じ代表点と空間的な近さを混ぜない | 座標のまったく同じレコードは「最近傍 0 m」にならず、次に近い**別の点**までの距離になる。共有していることは `shares_coordinates` で残す。**陽性対照**として `log1p(0) = 0` の縮退が起きないことを確かめる（実データに 181 件・80 組・6.5%） |
| T-053 | F-12 | 二次元配置の形 | 全レコードに座標があり、並びが埋め込みと一致し、有限で `-1..1` に収まる |
| T-054 | F-12 / G-24 | 近傍保存 | trustworthiness（k=20）が PCA-2 を上回る。SPEC §3.10 で測定前に宣言した規則に従う |
| T-055 | F-12 / G-25 / G-23 | 配置の陽性対照と、落としたものが戻らないこと | 座標を並べ替えた配置は 0.1 以上低い。記録した数値を**配った座標**から計算し直して一致する（丸めで 3.5e-6 動くので許容差はその実測から置く）。**G-23 不通過で落としたクラスタが黙って戻っていない** |
| T-056 | F-10 / G-26 | 類似検索の二実装照合（vitest） | 全 2,805 件で、TypeScript の上位 10 件が Python の出した照合表と **ID・順序とも一致**し、類似度の差が `1e-9` 以下。照合表は `kofun_atlas.features` を使わず、埋め込みの内積から Python 側で独立に作る。**時間制限は 60 秒**を明示する —— 既定の 5 秒は、他の重い処理と並行させると越えて時間切れで落ちた（実測 5,301〜5,440ms・単独なら 2,122ms。制限を 1 秒に縮めると同じ型で落ちることを陽性対照で確認）。固定の時間予算は正しさでなく混み具合を測る（HC-245）。延ばしても照合が骨抜きにならないことは T-057 が保証する |
| T-057 | F-10 / G-26 | 類似検索の陽性対照（vitest） | (a) 自分自身を除かない、(b) 類似度の昇順で並べる、(c) 同点をレコード順の降順にする —— の三つの変異体が、T-056 の照合で**それぞれ落ちる**。(c) が撃つ前提として、照合表の上位 10 件に同点が実在することを表明する。落ちない変異体があれば、その照合はその誤りを見ていない。変異体は**最初の食い違いで止める**（1 件食い違えば落ちたと言える。全件を回すと 1 本 13〜20 秒の同期処理になり、同じ実行の T-061 とワーカーの応答を時間切れにした・HC-278）。止める数え方が壊れていないことは、正しい実装を止める数え方に通して 0 件になることで確かめる |
| T-061 | F-13 | 詳細の分割鍵の言語間契約（vitest） | 出力された全分割ファイルの全 ID について、TypeScript の `shardOf` がその ID を実際に含むファイル名を返す。形の違う ID は例外にする。**規則がずれると型もテストも緑のまま、押した古墳の詳細だけが 404 になる**（HC-190）。実装の後に書いた検査である（stage 3 の赤確認は経ていない） |
| T-060 | F-16 | フリート共通フッタの宛先（vitest） | GitHub がこのリポジトリ（`github.com/twill3c/kofun-atlas-ai`）、App Menu が本番（`app-menu-amber.vercel.app`）、MIT License がリポジトリの `LICENSE` を指す。**期待値の出所は koho-lens/src/render.py 137〜141 行（フリートの正本・2026-09-14 参照）**。L0 で書いた宛先は実在しない URL だった |
| T-058 | F-13 | 地図用の資産 | GeoJSON の件数がレコード数と一致し、ID が一意で、座標が §7 G-02 の範囲に入る。詳細の分割ファイルを全部読むと全レコードがちょうど一度ずつ現れる（取りこぼし・重複 0） |
| T-059 | F-13 / G-15 / G-27 / G-28 | 実ブラウザ検品（`harness/smoke.mjs`） | 地図に点が描かれ、全件を視野に入れると描画数がデータ件数と一致し、キャンバスが器を埋め、点を押すと詳細が開き、似た立地の一覧が出て**強調した 10 件がすべて視野に入って描かれ**、5 つの画面幅（320 / 360 / 768 / 1280 / 1680 px）で横に溢れない（G-15 の三幅 320 / 768 / 1280 px は撮影して目視も行う）。**失敗は終了コードで知らせる**。陽性対照（層を隠す / データを空にする / 探す前は強調 0 件）を同じ実行の中で撃つ。**`querySourceFeatures` で数えない** —— 視野内に読み込まれたタイルの分しか返さず、タイル境界で重複もする（探針: 視野外で 0 件、寄せた後に実体 10 件を 16 件と返した）。データは `getData()`、描画は一意 ID の `queryRenderedFeatures` で数え、**検品器は自分では地図を寄せない**（寄せるのは画面の責任で、それを検査する） |
| T-062 | F-12 / G-30 | 範囲選択の二実装照合（vitest） | 照合表の矩形 6 つで、TypeScript の `selectInRect` が選ぶ点の番号集合が Python の独立実装と完全一致する。照合表の矩形に**辺ちょうどの点が実在する**こと・点 0 個の矩形と全点の矩形を含むことを表明する |
| T-063 | F-12 / G-30 | 範囲選択の陽性対照（vitest） | 辺を含まない実装・x と y を入れ替えた実装が T-062 の照合でそれぞれ落ちる |
| T-064 | F-12 / G-31 | 集計の二実装照合（vitest） | 都道府県ごとの件数と、標高・傾斜・開放度の中央値、分位の境界 4 つ、各クラスの件数が Python の独立実装と差 1e-9 以下で一致する |
| T-065 | F-12 / G-31 | 集計の陽性対照（vitest） | 平均で代用する実装・偶数個で下の真ん中を採る実装・境界の添字を切り上げる実装が T-064 でそれぞれ落ちる。偶数個の県が実在することを表明する |
| T-066 | F-12 | 探索用の資産 | `explore.json` の並び・座標・所在・地形の値が、二次元配置と点の GeoJSON と出荷レコードに一致する。値なしは `null` のまま |
| T-067 | F-12 / G-14 / G-29 | 探索画面の実ブラウザ検品 | 描いた点の数がデータ件数と一致し、各点の位置が配った座標と一致し（G-29）、図の中の要素がすべて `viewBox` に収まり（G-14）、範囲選択と県の強調が一覧表に反映される。陽性対照（1 点をずらす・枠外の文字を足す）を同じ実行の中で撃つ |
| T-068 | F-12 | 印の半径の規則（vitest） | 間隔の分かっている格子の点で、`overlapFraction` が閉形式どおりの割合を返し（直径が間隔を超えれば全点、下回れば 0）、`chooseRadius` が SPEC §3.14 の規則（4 → 3 → 2 と下げ、最初に 50% 以下になった半径・どれも超えれば最小）に従う。**実装の後に書いた検査**である（stage 3 の赤確認は経ていない） |
| T-069 | F-16 | ソースの和文の中の空白（pytest） | `src/` の `.ts` / `.tsx` の各行に「和文␣和文」が 0 件。報告は件数を先頭に出す。陽性対照として loop_006 で実際に出た形（「国土地理院 標高タイル」「重なりの割合 半径 2」）を拾い、正しい区切り（「半径 2: 87.7%」「標高 m」）を拾わない。JSX の改行が畳まれる型はソースの一行に現れないので T-059 / T-067 の検品器が担う |
| T-070 | F-16 / G-13 | 配る静的アセットの予算（pytest ＋ 検品器） | `public/` の合計が 60 MB 未満（pytest・CI の python ジョブでも走る）。`out/` の合計は、ビルドの後に必ず走る `harness/smoke.mjs` が測る。資産は既に上限を大きく下回るので実装前の赤は成り立たず、代わりに上限を実測値 −1 B / 実測値ちょうどにすると落ちる陽性対照を置く |
| T-071 | F-12 / F-13 / F-15 / F-16 | 実ブラウザのアクセシビリティ監査（`harness/smoke.mjs`） | 6 ページで、文書の言語が `ja`、`h1` がちょうど 1 つ、操作部品すべてに名前がある（placeholder だけは名前と数えない）、24 px 以上の図すべてに代替の説明がある、本文の文字が WCAG AA のコントラスト（通常 4.5・大きい文字 3.0）を満たす、Tab で焦点が移り焦点の輪が見える。背景は祖先の背景色の合成で求め、**地図の中は測らない**（真後ろはキャンバスで祖先の合成は誤る・HC-268）。背景画像に当たったら「測れない」として指摘する。陽性対照（名前の無いボタン・読めない文字・説明の無い図・焦点の輪を消す指定）を同じ実行で撃つ |
| T-072 | — | 本番の同一性（`SMOKE_BASE_URL` を付けた検品器） | 本番のデータ 5 ファイル（manifest・点・探索・埋め込み・ONNX）が、改行を揃えた sha256 で手元の `out/` と一致し、6 ページの描画された本文が手元の `out/` と一致する。**一致しなければ他の検査をせずに止める** —— 健やかさの検査は古い版にも通る（HC-148）。データの指紋はコードの新しさを言わないので本文も比べる |
| T-073 | F-15 / F-16 | 画面の散文の時制（pytest） | `src/app` と `src/components` のコメントでない行に、「これから作」「（予定）」「準備中」「段階的に作」「いまの状態」と、ループの段階名 `L0`〜`L9` が 0 件。L7 の公開直前にトップページが「いまの状態（L4）」「二次元配置の探索画面はこれから作ります」のまま残っていた（数値は manifest から読むので正しく変わり、隣の散文だけが古い・HC-280）。陽性対照は実際に残っていた形、拾わない形はコメントの経緯と英字に続く L |
| T-074 | G-08 / G-24 | 検査が読むモジュールの欠け（pytest） | `require_module` は、モジュールが無いとき手元では skip、`KOFUN_REQUIRE_ARTIFACTS=1` では失敗にする。在るモジュールはどちらでも返す。G-08 の ONNX 照合と G-24 の再計算はこの入口を通す —— `pytest.importorskip` を直接使うと CI で依存を入れ忘れても skip で緑になる（新しい clone に CI を再現して、ONNX 照合 3 件が CI で一度も走っていなかったことを見つけた） |
| T-075 | — | CI の pnpm の版の指定（pytest） | `package.json` の `packageManager` で pnpm の版を固定しているとき、`ci.yml` の `pnpm/action-setup` に `version` が無い。GitHub 上の最初の実行で web ジョブが「Multiple versions of pnpm specified」で落ちた。新しい clone での CI 再現は `run:` の行をなぞるだけで action の入力検査を再現できず、2 回とも緑だった。陽性対照は落ちた形そのもの（別の step の `with` は拾わない） |
| T-076 | F-12 | 散布図のタップの規則（vitest） | `nearestIndex` は描画単位 14 未満の最寄り点を返し（14 ちょうど・斜め 14.14 は外、同じ距離は番号の小さい方、点が無ければ null）、`isTap` は移動が x・y とも 2 未満のときだけ真。期待値は手で置いた座標の閉形式。陽性対照として上限を 15 に広げると 14 ちょうどの点を拾う |
| T-077 | F-12 | タッチ端末の探索画面（`harness/smoke.mjs`） | タッチ有効のモバイル端末で、散布図の点をタップするとその位置から 14 以内の古墳の名前が出て（SPEC の式から独立に計算）、指を離して 800 ms 後も残り、範囲選択の枠は出ない。陽性対照として最寄り点から 30 以上離れた位置をタップすると名前が消える。本番に当てた探針 `harness/_probe_touch.mjs` で、点をタップしても名前が出なかったことから置いた |
| T-027 | F-16 / G-01 | `data-manifest.json` の契約（vitest） | 画面が読む欄がすべてあり、件数どうしが辻褄の合う関係（`records + 統合行 = 元の行数`、`県あり + 県なし = 全件`、`覆した件 <= 県あり`、都道府県内訳の合計 = 県あり）になっている。**Python と TypeScript の境界は型の外にあるのでビルドでは守られない**（HC-152） |

### 未実装のゲート(SPEC §7 で明記済み)

`G-07` `G-09` `G-10` `G-17` は V1.0 では実装しない。`G-07` は照合相手(Wikidata / 文化庁のスナップショット)が置かれていない。`G-09` `G-10` `G-17` は時期分類器(F-11)を教師ラベル 0 件のため作らないので、測る対象が無い。
T-015 はこの列挙と SPEC の「未実装」表記が一致することを確かめる。

`G-07`(名寄せの false merge rate)は Wikidata / 文化庁のスナップショットが
置かれてからでないと測れない —— 照合相手が無い状態でゴールドセットは作れない。
